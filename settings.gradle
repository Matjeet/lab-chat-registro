plugins {
	// Permite a Gradle descargar automaticamente el JDK 25 de la toolchain si no esta instalado.
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'chat-registro'
